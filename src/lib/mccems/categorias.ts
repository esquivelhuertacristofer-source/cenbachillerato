/**
 * Categorías de progresión (Modelo MCCEMS 2025).
 *
 * Módulo neutral (sin dependencias de cliente ni servidor) para que tanto las
 * queries browser (`hub-browser.ts`) como las de servidor (`hub.ts`,
 * `progreso.ts`) compartan la misma etiqueta canónica sin importarse entre sí.
 *
 * El contenido marcado con esta categoría se conserva y se muestra como
 * "Contenido complementario · no oficial 2025", pero NO cuenta para el avance
 * (la meta son solo los propósitos formativos oficiales).
 */
export const CATEGORIA_COMPLEMENTO = "Complemento (no oficial 2025)";
